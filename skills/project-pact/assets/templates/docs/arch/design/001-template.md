---
title: Design Template 001
description: 一句话说明本设计文档覆盖的产品或功能模块。
type: design
# status: draft | active | archived
#   draft    → 编写中，尚未冻结（Designer 变更）
#   active   → 已冻结，当前唯一生效版本（Designer 变更）
#   archived → 被新版本替代，原地保留供追溯（Designer 变更）
status: active
version: 1
created: YYYY-MM-DDTHH:MM:SSZ
---

> [项目名称] 规格文档。所有业务需求、接口、数据模型、AC 均在此定义。
>
> <!--
> 本文件是单文件示意，展示完整的 Design 结构。实际编写时，应拆分为多篇文档：
> - 概要设计（一~三）保留在主文档
> - 详细设计（四~七）按模块独立为文件，主文档用引用链接
> - 验收与约束（八~十一）保留在主文档或按需拆分
>
> 拆分指南：
> - 如果某节的内容可独立阅读和理解（如接口按模块分组）→ 独立为文件
> - 如果某节内部有二级结构（如数据模型有多个实体）→ 独立为文件
> - 拆分后，本节改为引用链接，不复制内容
> - 不拆分的情况：内容简单、无内部结构、与上下文紧密耦合
>
> Design/ADR 边界：
> 以下内容属于 Design（WHAT，描述系统是什么）：
> - 接口定义、数据模型、业务规则、AC
> - 性能目标和安全要求
> 以下内容属于 ADR（WHY，记录为什么这样选择）：
> - 技术选型（框架、语言、协议、数据库）
> - 架构模式（分层策略、状态管理方案）
> - 存储格式（JSONL、SQLite、文件系统）
> 以下内容属于 CONTRIBUTING.md（HOW，编码规范）：
> - 语言版本（如 TypeScript 严格模式）
> - 具体 npm 包名和版本号
>
> 写作原则：
> - 不重复上层文档（Vision 已定义的内容不在此重述，引用即可）
> - 接口定义优先使用可生成代码的契约格式（如 OpenAPI、TypeScript 接口声明）
> - 模块职责 = 单一可验证的功能边界（不是"负责用户相关功能"，而是"提供用户注册、登录、信息查询接口"）
> -->

---

# 概要设计

## 一、项目概述

<!-- 简要描述本项目要构建什么，解决什么问题 -->

---

## 二、用户故事

<!-- 从用户视角描述核心场景 -->

| 编号 | 角色 | 需求 | 目的 | 优先级 |
|------|------|------|------|--------|
| US-001 | | | | P0 |

---

## 三、模块划分

| 模块 | 职责 | 优先级 |
|------|------|--------|
| | | P0 |

---

# 详细设计

## 四、接口定义

### 模块名称

#### `METHOD /api/xxx`

**请求：**
```json
{
  "field": "type"
}
```

**响应：**
```json
{
  "field": "type"
}
```

**错误码：**

| 状态码 | 含义 | 处理方式 |
|--------|------|----------|
| 200 | 成功 | — |
| 400 | 参数错误 | 返回错误提示 |
| 404 | 资源不存在 | 返回 404 页面 |

---

## 五、数据模型

### 表名

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | UUID | PK | 主键 |
| created_at | TIMESTAMP | NOT NULL | 创建时间 |

---

## 六、业务规则

<!-- 跨模块、跨接口的业务约束 -->

| 规则编号 | 描述 | 触发条件 | 约束 |
|----------|------|----------|------|
| BR-001 | | | |

---

## 七、UI 约束（前端项目必填）

### 页面结构

<!-- 页面路由、组件树、布局 -->

### 组件规范

<!-- 组件命名、Props 约定、状态管理。不写具体框架名和包名（属于 ADR/CONTRIBUTING）。 -->

### 视觉回归

<!-- 若有 Visual Regression Test 要求，在此定义基准和阈值 -->

---

# 验收与约束

## 八、验收标准（AC）

> 每条 AC 必须可量化、可自动化校验。
> **AC 是测试的唯一权威依据。** Executor 开发完成后，必须逐条验证 AC，全部通过方可标记 done。

### AC-001: [验收项标题]

- **前置条件**：
- **操作步骤**：
  1. 
  2. 
- **预期结果**：
- **校验方式**：自动化 / 手动 / Agent 判定
<!-- 自动化=机器可执行，手动=需要人操作，Agent 判定=Agent 通过 trace/截图给出结论 -->

### AC-002: [验收项标题]

- **前置条件**：
- **操作步骤**：
  1. 
- **预期结果**：
- **校验方式**：

---

## 九、非功能约束

> 继承自 [Vision](../vision.md)，此处仅补充本 Design 特有的约束。

| 维度 | 约束 |
|------|------|
| 性能 | |
| 安全 | |
| 数据合规 | |

---

## 十、依赖项

<!-- 本 Design 依赖的外部能力（不写具体包名，写需要什么功能） -->

| 依赖 | 版本 | 用途 |
|------|------|------|
| | | |

---

## 十一、术语表

| 术语 | 定义 |
|------|------|
| | |